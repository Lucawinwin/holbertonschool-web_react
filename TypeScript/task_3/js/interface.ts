// Create a type RowID and set it equal to number
type RowID = number;

// Create an interface RowElement with the specified fields
interface RowElement {
  firstName: string;
  lastName: string;
  age?: number; // Optional field indicated by the ?
}
